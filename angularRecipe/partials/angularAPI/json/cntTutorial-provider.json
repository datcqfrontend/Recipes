{ 
	"content":[
		{ 	"type": "subheader", 
			"title": "Provider: ",
			"shortContent": ""
		},

		{ 
			"title": "$anchorScrollProvider",
			"shortContent": "Use <code><span class='pln'>$anchorScrollProvider</span></code> to disable automatic scrolling whenever <a href='api/ng/service/$location#hash'>$location.hash()</a> changes.",
			"keyword": "",
			"link": "#angularAPI/provider/anchorScrollProvider"
		},
		{ 
			"title": "$animateProvider",
			"shortContent": "Default implementation of $animate that doesn't perform any animations, instead just synchronously performs DOM updates and resolves the returned runner promise.",
			"keyword": "",
			"link": "#angularAPI/provider/animateProvider"
		},
		{ 
			"title": "$compileProvider",
			"shortContent": "",
			"keyword": "",
			"link": "#angularAPI/provider/compileProvider"
		},
		{ 	"type": "clearfix"	},

		{ 
			"title": "$controllerProvider",
			"shortContent": "The $controller service is used by Angular to create new controllers.",
			"keyword": "",
			"link": "#angularAPI/provider/controllerProvider"
		},
		{ 
			"title": "$filterProvider",
			"shortContent": "Filters are just functions which transform input to an output. However filters need to be Dependency Injected. To achieve this a filter definition consists of a factory function which is annotated with dependencies and is responsible for creating a filter function.",
			"keyword": "",
			"link": "#angularAPI/provider/filterProvider"
		},
		{ 
			"title": "$httpProvider",
			"shortContent": "Use <code><span class='pln'>$httpProvider</span></code> to change the default behavior of the <a href='api/ng/service/$http'>$http</a> service.",
			"keyword": "",
			"link": "#angularAPI/provider/httpProvider"
		},
		{ 	"type": "clearfix"	},

		{ 
			"title": "$interpolateProvider",
			"shortContent": "Used for configuring the interpolation markup. Defaults to <code><span class='pun'>{{</span></code> and <code><span class='pun'>}}</span></code>.",
			"keyword": "",
			"link": "#angularAPI/provider/interpolateProvider"
		},
		{ 
			"title": "$locationProvider",
			"shortContent": "Use the <code><span class='pln'>$locationProvider</span></code> to configure how the application deep linking paths are stored.",
			"keyword": "",
			"link": "#angularAPI/provider/locationProvider"
		},
		{ 
			"title": "$logProvider",
			"shortContent": "Use the <code><span class='pln'>$logProvider</span></code> to configure how the application logs messages",
			"keyword": "",
			"link": "#angularAPI/provider/logProvider"
		},
		{ 	"type": "clearfix"	},

		{ 
			"title": "$parseProvider",
			"shortContent": "<code><span class='pln'>$parseProvider</span></code> can be used for configuring the default behavior of the <a href='api/ng/service/$parse'>$parse</a> service.",
			"keyword": "",
			"link": "#angularAPI/provider/parseProvider"
		},
		{ 
			"title": "$rootScopeProvider",
			"shortContent": "Provider for the $rootScope service.",
			"keyword": "",
			"link": "#angularAPI/provider/rootScopeProvider"
		},
		{ 
			"title": "$sceDelegateProvider",
			"shortContent": "The <code><span class='pln'>$sceDelegateProvider</span></code> provider allows developers to configure the <a href='api/ng/service/$sceDelegate'>$sceDelegate</a> service.  This allows one to get/set the whitelists and blacklists used to ensure that the URLs used for sourcing Angular templates are safe.  Refer <a href='api/ng/provider/$sceDelegateProvider#resourceUrlWhitelist'>$sceDelegateProvider.resourceUrlWhitelist</a> and <a href='api/ng/provider/$sceDelegateProvider#resourceUrlBlacklist'>$sceDelegateProvider.resourceUrlBlacklist</a>",
			"keyword": "",
			"link": "#angularAPI/provider/sceDelegateProvider"
		},
		{ 	"type": "clearfix"	},

		{ 
			"title": "$sceProvider",
			"shortContent": "The $sceProvider provider allows developers to configure the <a href='api/ng/service/$sce'>$sce</a> service. <ul> <li>enable/disable Strict Contextual Escaping (SCE) in a module</li> <li>override the default implementation with a custom delegate</li> </ul>",
			"keyword": "",
			"link": "#angularAPI/provider/sceProvider"
		},
		
		{ 	"type": "clearfix"	}

		
	]
}